[package]
name = "msdelta-bsdiff"
version = "0.0.0"
edition = "2021"
rust-version = "1.75"
license = "MIT OR Apache-2.0"
description = "BsDiff binary delta codec for MSDelta's PlaybackReverse path."

[dependencies]
thiserror = "2"
